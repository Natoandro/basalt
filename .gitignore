.DS_STORE
xcuserdata/
**/node_modules
.yarn/*
.turbo/*
turbo-build.log
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions
.pnp.*
dist/
.eslintcache
.graphite_repo_config
.idea/*
.nyc_output/
coverage/
pkg/

# we use yarn, not npm
package-lock.json

# Rust / Cargo
/target
Cargo.lock
**/*.rs.bk
*.pdb

# basalt team-wide config (optional, can be committed if desired)
# Metadata is stored in .git/basalt/ which is never committed
.basalt.toml
